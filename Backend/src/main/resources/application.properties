
# APPLICATION
spring.application.name=message-service
server.port=8080

#POSTGRESQL
#spring.datasource.url=
#spring.datasource.username=
#spring.datasource.password=
#spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / HIBERNATE
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# SESSION
server.servlet.session.timeout=30m

# WEBSOCKET
#websocket.max-connections-per-user=5
#websocket.allowed-origins=TODO:url del front
#websocket.heartbeat.interval=25000
#websocket.heartbeat.timeout=60000

# LOGGING
logging.level.org.springframework.orm.jpa=DEBUG
logging.level.org.springframework.transaction=DEBUG
logging.level.com.message=DEBUG
logging.pattern.console=%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n
logging.file.name=logs/message-service.log

# SECURITY
#NO OLVIDAR ESTA CONTRASEÑA
jwt.secret=GioTld3005
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# CORS
#cors.allowed-origins=TODO:url del front
#cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
#cors.allowed-headers=*
#cors.allow-credentials=true

# ACTUATOR y MONITORING
#management.endpoints.web.exposure.include=health,info,metrics,prometheus
#management.endpoint.health.show-details=when-authorized
#management.info.env.enabled=true
#management.metrics.export.prometheus.enabled=true

# PERFORMANCE y OPTIMIZATION
server.tomcat.max-connections=8192
server.tomcat.threads.max=200
server.tomcat.threads.min-spare=10
server.tomcat.connection-timeout=20000

# JACKSON JSON
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jackson.time-zone=America/Santiago
spring.jackson.default-property-inclusion=NON_NULL

# VALIDATION y ERROR HANDLING
spring.mvc.throw-exception-if-no-handler-found=true
spring.web.resources.add-mappings=false
server.error.include-stacktrace=on-param
server.error.include-message=always

# PROFILE CONFIGURATION
spring.profiles.active=dev
